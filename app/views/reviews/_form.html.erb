


<%= f.hidden_field :user_id, value: current_user.id %>
<%= f.hidden_field :product_id, valude: @product.id %>

<%= f.label :rating %>
<%= f.select :rating, options_for_select(1..10) %><br>

<%= f.label :comment %>
<%= f.text_area :comment %>